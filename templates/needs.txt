const socket = io('/client');

socket.on('connect', () => {
console.log("✅ WebSocket 연결됨");
});

socket.on('redirect', (data) => {
console.log("🔁 서버로부터 전환 요청:", data.url);
window.location.href = data.url;
});